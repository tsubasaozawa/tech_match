= link_to user_path(current_user) do
  プロフィール
%br
= link_to destroy_user_session_path, method: :delete do
  ログアウト
%br
- if current_user.admin?
  = link_to admin_member_users_path do
    管理者ページ
%br
ユーザー名：
= current_user.nickname
%br
%span{style: "font-size:18px;"} ユーザー一覧
%br
- @users.each do |user|
  = link_to "#{user.nickname}",  user_path(user)
  - if user.profile_image.blank?
    = image_tag "https://static.mercdn.net/images/member_photo_noimage_thumb.png", class: "profile-image"
  - else
    = image_tag user.profile_image.url, class: "profile-image"
%br
%span{style: "font-size:18px;"} マッチングユーザー一覧
%br
- @matched_users.each do |matched_user|
  = link_to "#{matched_user.nickname}",  user_path(matched_user)
  - if matched_user.profile_image.blank?
    = image_tag "https://static.mercdn.net/images/member_photo_noimage_thumb.png", class: "profile-image"
  - else
    = image_tag matched_user.profile_image.url, class: "profile-image"
%br
%span{style: "font-size:18px;"} フォローしているユーザー一覧
%br
- @users.each do |user|
  - if current_user.following?(user)
    = link_to "#{user.nickname}",  user_path(user)
    - if user.profile_image.blank?
      = image_tag "https://static.mercdn.net/images/member_photo_noimage_thumb.png", class: "profile-image"
    - else
      = image_tag user.profile_image.url, class: "profile-image"

%br
%span{style: "font-size:18px;"} フォローされているユーザー一覧
%br
- @users.each do |user|
  - if current_user.followers?(user)
    = link_to "#{user.nickname}",  user_path(user)
    - if user.profile_image.blank?
      = image_tag "https://static.mercdn.net/images/member_photo_noimage_thumb.png", class: "profile-image"
    - else
      = image_tag user.profile_image.url, class: "profile-image"